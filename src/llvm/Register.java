package llvm;


import Symbol.Symbol;

public class Register {
    public int registerNumber;

    public Register() {
        int registerNumber;
        for (Symbol i : Compiler.Compiler)
        this.registerNumber = registerNumber;
    }
}
